apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "status-page-chart.fullname" . }}-test-connection"
  labels:
    {{- include "status-page-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "status-page-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
