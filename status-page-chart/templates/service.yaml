apiVersion: v1
kind: Service
metadata:
  name: {{ include "status-page-chart.fullname" . }}
  labels:
    {{- include "status-page-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "status-page-chart.selectorLabels" . | nindent 4 }}
